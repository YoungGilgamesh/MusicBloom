# Cover page images

Drop reference/visual assets for the cover (attract-mode) page here — e.g.
logo, background artwork, reference screenshots, etc.

Files placed in this folder are served statically at:

```
/images/cover/<filename>
```

(Vite serves everything under `public/` from the site root — same pattern as
`public/audio/` and `public/models/`.)
